<%= form_for(video2tag) do |f| %>
  <% if video2tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video2tag.errors.count, "error") %> prohibited this video2tag from being saved:</h2>

      <ul>
      <% video2tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :string %>
    <%= f.text_field :string %>
  </div>

  <div class="field">
    <%= f.label :string %>
    <%= f.text_field :string %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
